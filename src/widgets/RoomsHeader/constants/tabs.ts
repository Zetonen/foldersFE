import type { RoomsTab } from '@/types'

/** FR-ROOMS-01: the two tabs of the data room list. */
export const TAB_LABELS: Record<RoomsTab, string> = {
  my: 'My data rooms',
  shared: 'Shared with me',
}
